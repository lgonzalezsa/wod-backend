    #OS install via ILO: using ubuntu-20.04.1-live-server-amd64.iso
    # IP address : 
	englisk keyboard
	network:
	IP address : 16.31.85.200
	broadcast : 16.31.84.0/22
	GW: 16.31.84.1
	DNS: 16.31.84.24
	search etc.fr.comm.hpecorp.net
	Disk : manual / reformat puis use entire disk
	no proxy
	user creation user jupyter / P@ssw0rd
	ssh server ok

Autoinstall with ansible and sshd

cleanup potential old apt setup
cleanup potential /usr/local/{bin,lib,go} content
stop a potentially running jupyter service
setup jupyter account (or rename previous one)
setup ssh
ssh-keygen -t rsa -b 4096
add id_rsa.pub in authorized_keys
ssh localhost ls
copy ssh keys from other jupyterhub servers
id_jupproc
id_student0
chmod 600 id_*
chmod 644 id_*.pub
setup sudo:
sudo visudo
  jupyter ALL=(ALL) NOPASSWD:ALL

Install Ansible
sudo apt install ansible -y

GIT_SSH_COMMAND='ssh -i ~/.ssh/id_jupproc -v ' git clone git@github.com:HPEDevCom/jupyter-procmail.git
cd jupyter-procmail
./scripts/install_jupyter.sh


#### OLD ####
Priorities: rust, java, go

    #Install conda for the whole system
    sudo ln -s /opt/conda/etc/profile.d/conda.sh /etc/profile.d/conda.sh
    sudo mkdir /opt/conda/envs/
    sudo /opt/conda/bin/conda create --prefix /opt/conda/envs/python python=3.7 ipykernel
    sudo /opt/conda/envs/python/bin/python -m ipykernel install --prefix=/opt/jupyterhub/ --name 'python' --display-name "Python (default)"
